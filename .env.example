# OpenAI Configuration
# Create a .env file on the root directory.

# Required: Your OpenAI API key for LLM and TTS functionality
OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenAI model to use (default: gpt-4)
# Available models: gpt-4, gpt-4o, gpt-4-turbo, gpt-4o-mini, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o

# Model Provider Selection
# Required: Choose between "openai" or "ollama"
# - "openai": Use OpenAI's API (requires OPENAI_API_KEY)
# - "ollama": Use local Ollama models (requires Ollama running locally)
MODEL_PROVIDER=ollama

# Ollama Configuration (only needed if MODEL_PROVIDER=ollama)
# Optional: Ollama model name (default: llama3.1)
# Make sure the model is installed: ollama pull llama3.1
OLLAMA_MODEL=llama3.1

# Serper API Configuration
# Required: Your Serper API key for web search functionality
# Get your API key at: https://serper.dev/
SERPER_API_KEY=your_serper_api_key_here
